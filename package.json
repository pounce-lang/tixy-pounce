{
  "name": "plexy-glass",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^11.1.0",
    "@rollup/plugin-node-resolve": "^7.1.3",
    "rollup": "^2.10.9",
    "serve": "^11.3.2"
  },
  "dependencies": {
    "@pounce-lang/core": "^2.0.4"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "dev": "npm-run-all --parallel start watch",
    "start": "rollup -c -w & serve "
  }
}
